# Block chain config

name: valid_blockchain
# block mining difficulty
difficulty: 4
# miner reward
reward: 10
# test mode false, does not dump or read blockchain file
testMode: false

# file dump configs
dump:
  dir: data/valid
  # if filename is null, use blockchain name
  file: null
